@page "/pizzeria"
@using KrisNikShopProject.Components
@using System.ComponentModel.DataAnnotations
@rendermode InteractiveServer


<EditForm Model="CurrentPizza">
    <DataAnnotationsValidator />
    <ValidationSummary />

    <InputText id="Name" class="form-control" @bind-Value="CurrentPizza.Name" />
    <ValidationMessage For="(()=> CurrentPizza.Name)" />
</EditForm>

<!-- Rest of your code... -->
@code {
    public Pizza CurrentPizza { get; set; }
    public bool ShowModal { get; set; }

    public void OnClick()
    {
        ShowModal = true;
        StateHasChanged();
    }
    List<Pizza> specials = new();
    protected override void OnInitialized()
    {
        CurrentPizza = new Pizza();
        specials.AddRange(new List<Pizza>
        {
            // Your pizza data...
        });
    }

    public class Pizza
    {
        [Required]
        public string Name { get; set; }
        public string Description { get; set; }
        public string ImageUrl { get; set; }
        public decimal BasePrice { get; set; }
    }
}